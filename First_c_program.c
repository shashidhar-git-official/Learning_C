#include <stdio.h> //this is a standard input/output headder file of C

int main (){       //this is the entry point for any program in C

    printf("hello world!\n");                //this is the print statement in C
    printf("This is shashidhar\n");
    return 0;                                /*main function is expected to return an integer, 0 indicates the program ran sucessfully. a non-zero value typically indicates an error.
                                             in newer versions of C we can skip return 0, but its good practice to keep for backwords compatibility */
}